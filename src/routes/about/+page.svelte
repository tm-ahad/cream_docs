<script>
    import Navbar from "../navbar.svelte"
</script>
<Navbar/>
<div class="section">
    <h1>cream</h1>
    <p>A compiler that compiles to html &amp; let you write frontend UI with declarative code</p>
    <p>features</p>
    <ul class="list-disc ml-4p">
        <li>Compiled javascript with zero abstraction</li>
        <li>All js and markup in one html file</li>
        <li>Reactive states without weird apis</li>
        <li>Very small js script with zero dependencies</li>
        <li>Stdlib for fetch, catch, extract, hash and cycle data</li>
        <li>Build full stack web apps with any backend framework</li>
        <li>No node_modules required for running cream </li>
        <li>Blazingly fast and memory safe cause build with <strong>Rust</strong></li>
        <li>Relatively newer syntax</li>
        <li>No bundler</li>
    </ul>
    <p class="mt-2p">Installaion:</p>
    <p class="mb-opsp">To install latest version of cream on Mac or linux or on Windows(msys), run this command</p>
    <pre><code class="lang-shell code">curl https:<span class="hljs-regexp">//</span>raw.githubusercontent.com<span class="hljs-regexp">/tm-ahad/</span>cream-installation<span class="hljs-regexp">/master/i</span>nstall.bash | bash</code></pre>
    <p class="mt-opsp"><strong>Note</strong>: You&#39;ll have to run the command on a <a href="https://www.msys2.org/">msys</a> shell on windows to install cream.</p>
</div>